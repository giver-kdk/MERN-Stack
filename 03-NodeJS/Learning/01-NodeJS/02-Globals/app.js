console.log(__dirname);				// Returns directory of this app 
console.log(__filename);			// Returns this app's filename
console.log(require); 				// Function to use modules. Also, contains the modules in it 
console.log(module); 				// Allows the contents to be exported
console.log(process); 				// Has info about current env where app is running